// function main() {}
تابع سیلک {
    // index = 1
    شمارنده = ۱
    // while (index != 101) {}
    تا شمارنده != ۱۰۱ {
        // print index
        نمایش شمارنده
        // index = index + 1
        شمارنده = شمارنده + ۱
    }
}
