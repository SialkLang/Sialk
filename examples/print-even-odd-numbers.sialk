تابع سیلک {
    عدد = ۱

    تا عدد != ۱۰۰ {
        اگر عدد ٪ ۲ == ۰ {
            نمایش عدد
        }
        عدد = عدد + ۱
    }

    نمایش "===================="

    عدد = ۱
    تا عدد != ۱۰۰ {
        اگر عدد ٪ ۲ != ۰ {
            نمایش عدد
        }
        عدد = عدد + ۱
    }
}
