// function main {}
تابع سیلک {
    // value = 1
    مقدار = ۱
    // print value
    نمایش مقدار
    {
        // print value
        نمایش مقدار
        {
            // print value
            نمایش مقدار
            // value = value + 1
            مقدار = مقدار + ۱
            // print value
            نمایش مقدار
        }
        // print value
        نمایش مقدار
    }
    // print value
    نمایش مقدار
}
